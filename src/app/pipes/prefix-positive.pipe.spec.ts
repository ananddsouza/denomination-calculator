import { PrefixPositivePipe } from './prefix-positive.pipe';

describe('PrefixPositivePipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixPositivePipe();
    expect(pipe).toBeTruthy();
  });
});
